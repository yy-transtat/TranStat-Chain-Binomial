% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{gen_population}
\alias{gen_population}
\title{Generate a Pseudo-population for Chain Binomial Simulation}
\usage{
gen_population(
  n_community,
  community_size,
  day_epi_stop,
  case_ascertained = 0L,
  cluster_randomization = 0L,
  seed = 123456789L,
  prop_idx = 0.8,
  prop_contact = 0.5
)
}
\arguments{
\item{n_community}{Integer. Number of independent communities (e.g., households).}

\item{community_size}{Integer. Number of individuals per community.}

\item{day_epi_stop}{Integer. Last day of the epidemic follow-up period.}

\item{case_ascertained}{Integer (0 or 1). If 1, the first member of each
community is pre-assigned as the index case with illness onset on day 1
(case-ascertained design). Default 0.}

\item{cluster_randomization}{Integer (0 or 1). If 1, all index cases in a
community receive the same treatment assignment, and all contacts receive
a (potentially different) shared assignment. If 0, each individual is
randomized independently. Default 0.}

\item{seed}{Integer. Random seed. Default 123456789L.}

\item{prop_idx}{Double. Probability that an index case is in the control
group (untreated). Default 0.8.}

\item{prop_contact}{Double. Probability that a contact is in the control
group (untreated). Default 0.5.}
}
\value{
A named list with three data frames:
\describe{
\item{pop}{One row per person. Columns: \code{id}, \code{community},
\code{pre_immune}, \code{infection}, \code{symptom}, \code{day_ill},
\code{exit}, \code{day_exit}, \code{idx}, \code{u_mode}, \code{q_mode},
\code{weight}, \code{ignore}, \code{time_ind_covariate}.}
\item{community}{One row per community. Columns: \code{community},
\code{day_epi_start}, \code{day_epi_stop}, \code{day_last_followup},
\code{c2p_group}.}
\item{time_dep_covariate}{Long-format time-dependent covariate (standard
normal). Columns: \code{id}, \code{day}, \code{value}.}
}
}
\description{
Creates a synthetic population of communities, assigns treatment
covariates, and seeds index cases. This is the first step in a
chain binomial simulation study. The population can then be passed
to the TranStat estimation routines.
}
\examples{
\dontrun{
pop <- gen_population(
  n_community       = 100,
  community_size    = 5,
  day_epi_stop      = 14,
  case_ascertained  = 1L,
  cluster_randomization = 0L
)
head(pop$pop)
}

}
